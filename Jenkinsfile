pipeline {
    agent any

    stages {
        
        stage('Build') {
            steps {
                // Build your Maven project
                sh 'mvn clean package'
            }
        }
        
       

    }
}
